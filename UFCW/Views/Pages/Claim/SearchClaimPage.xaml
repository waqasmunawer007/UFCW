<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
		xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
		xmlns:local="clr-namespace:UFCW;assembly=UFCW"
		Title="Search"
		x:Class="UFCW.SearchClaimPage">
	<ContentPage.Content>
	    <StackLayout Orientation = "Vertical"  Padding="0,10,0,10">
			<!-- Search Criteria title -->
		     <Label Font="Bold" Margin="10,0,0,0" VerticalTextAlignment="Center" TextColor="{ DynamicResource BaseTextColor }" FontSize="{DynamicResource BaseFontSize}" Text="Search Criteria"/>
		     <!-- Single Line -->
			<BoxView WidthRequest="1000" HeightRequest="1" BackgroundColor="{ DynamicResource BaseTextColor }" />
			 <!-- Filters dropdown grid-->
	            <Grid 
	                ColumnSpacing="5" 
	                RowSpacing="2" 
	                Padding="10,2,10,10"
	                VerticalOptions="Start" >
	                <Grid.RowDefinitions>
	                    <RowDefinition Height="*" />
	                </Grid.RowDefinitions>
					<!--Cliam Type filter -->
						 <Picker  Grid.Row="0" Grid.Column="0" x:Name="TypePicker" Title="Select claim type">
						  <Picker.ItemsSource>
						    <x:Array Type="{x:Type x:String}">
						      <x:String>Baboon</x:String>
						      <x:String>Capuchin Monkey</x:String>
						      <x:String>Blue Monkey</x:String>
						      <x:String>Squirrel Monkey</x:String>
						      <x:String>Golden Lion Tamarin</x:String>
						      <x:String>Howler Monkey</x:String>
						      <x:String>Japanese Macaque</x:String>
						    </x:Array>
						  </Picker.ItemsSource>
						</Picker>
					<!--Cliam Status filter -->
						 <Picker Grid.Row="0" Grid.Column="1" x:Name="StatusPicker" Title="Select claim status"/>
					 <!--Patient filter -->
						 <Picker  Grid.Row="1" Grid.Column="0" x:Name="PatientPicker" Title="Select Patient"/>
					 <!--Dependent's First Name filter -->
						 <Picker Grid.Row="1" Grid.Column="1" x:Name="DependentPicker" Title="Select depedent"/>
					 <!--Claim Services Date Range filter -->
						 <Picker Grid.Row="2" Grid.Column="0" x:Name="DateRangePicker" Title="Select date range"/>
	            </Grid>
		</StackLayout>
	</ContentPage.Content>
</ContentPage>
