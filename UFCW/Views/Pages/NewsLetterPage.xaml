<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:local="clr-namespace:UFCW;assembly=UFCW"
    x:Class="UFCW.Views.Pages.NewsLetterPage">
    <ContentPage.Resources>
         <ResourceDictionary>
            <local:HTMLSourceConverter x:Key="htmlSource" ></local:HTMLSourceConverter>
        </ResourceDictionary>
    </ContentPage.Resources>
    
	<ContentPage.Content>
        <StackLayout VerticalOptions = "FillAndExpand" Padding="10,20,10,10" HorizontalOptions = "FillAndExpand" Orientation="Vertical">
            <ActivityIndicator IsVisible="{Binding IsBusy}" IsRunning="{Binding IsBusy}"
                                        VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand"/>
            <ListView x:Name="NewsLetterList" HasUnevenRows="true" ItemTapped="NewsLetterList_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Margin="0,3">
                                <Label Text="{Binding Title}" VerticalTextAlignment="Center" FontSize="Medium"  HeightRequest="60" BackgroundColor="Blue" TextColor="White" ></Label>
                                <WebView  IsVisible="{Binding IsVisible}" Source="{Binding Template, Converter={StaticResource htmlSource}}" />
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
         </StackLayout> 
	</ContentPage.Content>
</ContentPage>
